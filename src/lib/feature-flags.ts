/**
 * Presentation-only flags. Backend gamification APIs can stay enabled.
 *
 * Set `SHOW_GAMIFICATION_UI` to `true` when you want badges, streaks, points,
 * weekly goals, and related copy visible again (future iteration).
 */
export const SHOW_GAMIFICATION_UI = false;
